import React from 'react';
import { View, Text } from 'react-native';

const HardModeScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Hard Mode Game Screen</Text>
        </View>
    );
};

export default HardModeScreen;