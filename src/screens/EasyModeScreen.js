import React from 'react';
import { View, Text } from 'react-native';

const EasyModeScreen = ({ navigation }) => {
  return (
    <View>
      <Text>Easy Mode Game Screen</Text>
    </View>
  );
};

export default EasyModeScreen;